#redis implementation using repository method without spring annotations repo calls for cashed data

#added new crud operation which stores data in cache and db with spring cache annotations and which is better approach than repo call
